
# MCP servers configuration
mcp_servers:
  # Local math MCP server (mcp_server.py)
  math:
    description: "Math operations MCP server"
    transport: "stdio"
    command: "python"
    args:
      - "-m"
      - "aisbot.mcp_server"
